<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:background="@color/orange_500"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:gravity="bottom"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:layout_height="@dimen/nav_header_height">
    <!--
    1) app:srcCompat so foi acessado quando o xmlns:app="http://schemas.android.com/apk/res-auto"
     foi adicionado no LinearLayout
    2)  android:theme="@style/ThemeOverlay.AppCompat.Dark" faz com que o container seja tratado
    como escuro e dessa forma o texto do icone deixa de ser preto e muda para claro
    3) android:contentDescription="@null" -> Essa propriedade foi indicada como atencao no imageView
    a fim de ser colocado uma descricao, sempre que nao se quiser esconder uma propriedade, pode-se
    setar o valor dela como nullo
     -->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher_round"
        android:contentDescription="@null" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="@string/app_name"/>

</LinearLayout>